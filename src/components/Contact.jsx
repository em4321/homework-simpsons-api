import React from "react";

const Contact = (props) => {
  return (
    <div
      className="contact"
      style={{
        textAlign: "center",
        marginTop: "300px",
      }}
    >
      Contact Page
    </div>
  );
};

export default Contact;
